H = []
EDS = {}
def node(start, end):
  if start = end:
    H += (start, start)
  mid = (start + end) // 2
  left_child = node(start, mid)
  right_child = node(mid+1, end)
  H += left_child
  H += right_child

node(1, LOG.max)
for node in H:
  for v in range(1, LOG.max):
    CF += 
    EDS[node] = CF
  
  
     
